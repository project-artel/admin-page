export function App() {
  return (
    <main
      style={{
        padding: 'var(--space-8)',
        color: 'var(--color-text-primary)',
      }}
    >
      <h1>ARTEL Admin</h1>
      <p style={{ color: 'var(--color-text-secondary)' }}>
        Design tokens loaded from <code>src/styles/tokens.css</code>.
      </p>
    </main>
  )
}
